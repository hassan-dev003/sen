# dependencies
/node_modules
.pnp
.pnp.*

# next
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# testing
/coverage

# env — never commit real secrets
.env
.env.local
.env*.local

# supabase local
/supabase/.temp
/supabase/.branches

# real financial data — never commit (AGENTS.md "Working with real bank data")
# the two synthetic extract-layer fixtures are the only PDFs allowed in this repo
*.pdf
!tests/fixtures/**/*.pdf
/captures/
/statements/

# misc
.DS_Store
*.log
